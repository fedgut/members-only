post form

<%= form_with model: @post, local: true do |f| %>

<%= f.label 'Title' %>
<%= f.text_field :title %> <br>

<%= f.label 'Body'%>
<%= f.text_area :body %> <br>

<%= f.submit 'Post'%>

<% end %>